# 中文可玩基础验收记录

日期：2026-09-06。状态：执行中；尚未宣告 M0 完成。

## 环境和原始基线

- 独立工作树 `.worktrees/chinese-foundation`，分支 `codex/chinese-foundation`，起点 `fc589ab`。
- Node.js22.23.2；保留原锁文件。媒体通过 Git LFS 恢复，`npm run verify:assets` 验证全部38项资源。
- 原始单元测试49套/440项通过。工作树祖先目录命中原 Jest 排除式的问题，在任务1中修复为相对 rootDir 的排除式。
- 构建前资源缺失问题恢复后，生产页面可以运行。原有 webpack3组体积警告保留。

## 已验收：海上存档恢复

提交 `b4f0669`。创建海上世界时立即初始化风和洋流，保留派生数据不入存档的边界。

- 新增3项实际 load/createWorld 回归：220分钟开局、240→260非更新时刻启动/绘制、另一海区读档覆盖旧风流。
- RED3项预期失败，修复后 GREEN3项通过；全部 Jest50套/443项通过。
- 类型检查、ESLint、格式检查和生产构建通过。
- 相关 Edge 浏览器回归5项通过；随后控制端运行完整英文 E2E，14套/57项全部通过，耗时3分15秒。包含真实新游戏走完里斯本开场、补给和出海的旅程。
- 独立 Sol 审查批准，无严重、重要或次要发现。

本机完整浏览器验证使用 `npx cypress run --browser edge`，仓库 CI 仍使用 Chrome。Cypress10.10在此 ARM macOS 上有非致命的 term-size二进制警告，测试进程退出码为0。

## 待验收：中文显示与整合

- 默认简体中文、语言偏好独立保存、中英切换保留进度。
- 当前全部可见入口与里斯本开场文本覆盖，无误改菜单 action、剧情 ID 或玩家命名。
- 中文真实开局、交易、补给、出海、保存/读取，英文全部回归。
- 1700×1000浏览器视口下的固定1640×800游戏布局、中文断行和对话选项检查。
- 全分支审查及最终证据。

## 范围说明

本记录只验收 M0。后续章节、战斗、约翰结局和其余主角路线分别属于 M1–M4，不因基础中文化通过而宣告完成。
