# M1 首次航海验收记录

日期：2026-09-06。分支：`codex/m1-first-voyage`。基线：`87e3319`。最终产品代码：`c07b34d`。

本文保留阶段验收快照。用户随后授权交接、合并提交与推送；后续集成状态及 M2 接续事项见根目录[交接文档](../../../HANDOFF.md)。

## 交付范围

里斯本开场之后，玩家可以在工会接受首次航海委托，连续出海三天后遇见多明戈，绘制直布罗陀海峡并返港上报，完成本章。拒绝委托后可重新接取；拒绝多明戈后可在里斯本工会重新邀请。日志给出当前步骤、沿岸航向和地理坐标。

直布罗陀海峡发现时增加30冒险名声，上报支付300金币；委托完成支付500金币。500金币委托是本项目原创玩法设计。多明戈的后续身份揭晓、决斗与完整约翰主线属于后续阶段。

存档升级为v5，新增语义事件完成记录与已上报发现列表。v4的发现物已经领取过金币，迁移后全部标记为已上报；未知旧任务键和语义ID保留。未结束的对话读档后重新开始，已完成的奖励和招募不会重复执行。

## 验证环境

- macOS，Node.js22.23.2，仓库锁定依赖与真实LFS图像、地图和音频资源。
- M1生产构建运行在 `http://127.0.0.1:8081/`，保留已有8080预览。
- Cypress10.10.0 / Microsoft Edge152，1700×1000桌面视口，实际游戏区域1640×800。
- 项目仍使用固定桌面布局，小窗口需要横向滚动。

## 自动化证据

M1验收通过。中文完整旅程从空存档实际走完开场、招募与补给、工会接取委托、沿岸往返航行、多明戈加入、发现上报及领取奖励；读取后进度和奖励不重复。已完成：

| 检查 | 结果 | 本机日志 |
| --- | --- | --- |
| 最终 `npm run verify` | 38项资源预检、剧情校验、69组530项Jest、类型检查、ESLint、生产构建全部通过 | `/tmp/uw2-m1-verify-backdrop.log` |
| 定向Cypress | 10项通过；中英文两项长旅程留给全量运行 | `/tmp/uw2-m1-reviewed-focused.log` |
| `325c882`全量Cypress | 16份规格67项，66通过；中文长旅程暴露退场遮罩拦截点击，随后修补 | `/tmp/uw2-m1-e2e-reviewed-full.log` |
| `c07b34d`修补后Cypress | 8项通过、0失败；5项已通过的英文/旧档用例按标志不重复执行。中文空存档完整旅程595秒通过 | `/tmp/uw2-m1-e2e-backdrop-final.log` |

生产服务使用 `npx http-server build -a127.0.0.1 -p8081 --silent -c-1`；全量浏览器命令：

```sh
npx cypress run --browser edge --config baseUrl=http://127.0.0.1:8081,trashAssetsBeforeRuns=false
```

定向运行在上述命令追加 `--spec tests/e2e/firstVoyage.cy.ts,tests/e2e/hudLayout.cy.ts,tests/e2e/storyArchitecture.cy.ts --env m1FixturesOnly=true`。遮罩修补后的同组运行使用 `--env m1Only=true`，保留中文完整旅程，跳过刚通过的英文长旅程和4个旧存档用例。全量命令没有设置跳过标志。使用Node22执行，浏览器不抑制未捕获异常。分开记录各次运行，不把修补前的全量结果冒称为最后提交的全量重跑。

已经完成的分层检查：

- Task1复审批准：语义进度、旧键映射、v4迁移和独立迁移清单校验。
- Task2复审批准：真正的模拟入口暂停测试、挂载的海上对话控件、实际单例保存/读取、失败读档保持会话、过期控件失效。删除暂停判断或读取订阅的两项变异检查均使对应测试失败。
- Task3复审批准：声明式新章、发现上报、日志、双语覆盖与四名伙伴资料。
- 整合阶段新增4项失败测试重现侧栏关闭和键盘输入穿透，修补后通过。侧栏关闭不会退出底层建筑；菜单、确认框和对话不会收到被侧栏遮挡时的确认输入。
- 浏览器分支测试通过中文海上读档/拒绝/返港招募/上报800金币和英文旧存档拒绝重接/新章500金币；两条都验证读取后不重复发钱。
- 4个真实v2存档用例通过：开场定位、部分进度续玩，以及港口是/否分支后的伙伴角色和完成记录。初始保存明确使用v2，迁移结果通过真实「系统→保存」检查，未把旧字段伪装成v5存档。
- 手动按住方向键关闭弹窗后，自动重复输入曾导致移动锁住；回归先失败，新增按键释放保护后通过。第4艘船曾触发旧面板位置越界；4/9艘真实组件测试先失败，修补后通过，浏览器实际买入第4艘、分配多明戈为船长并打开舰队也通过。
- 导航测试使用真实方向键与系统保存来观测位置，不注入运行时变量。夜间在港内行走避免行人自动绕行改变测试的像素位置模型；海上按实际移动步幅控制转弯，往返后10名水手和剩余补给得到验证。
- 中文长旅程的快速导航暂停暴露了退场动画遮罩残留：面板关闭、遮罩透明，仍会拦截点击。`c07b34d`让遮罩在关闭时立即释放指针输入，保留淡出动画；新增保留退场节点的回归先失败再通过，真实浏览器连续25次保存/关闭/重开也通过。没有强制点击或关闭浏览器异常检测来掩盖问题。
- 最终中文空存档验收：开场获得2000金币，招募10人花400，购买30份食物花600，出航时1000金币；海峡发现后金币仍1000、冒险名声30；返港上报300及委托500后金币1800。实际存档为v5，海峡只上报一次、多明戈只有一名、本章完成事件只有一条；重新加载后1800金币保持不变。

## 人工界面检查

四名伙伴面板选择多明戈后，头像、17岁资料、能力条、姓名和列表均完整显示。新增日志造成的旧弹窗顶部裁切已经修补，所有侧栏弹窗按游戏区域居中。日志中的导航提示使用南下后向东的海岸路线及经纬度，避免显示内部地图格坐标。超过3艘船使用四列可滚动展示；实际4艘船截图已目检。海上及港内HUD高度均通过实际浏览器测量。

实际游戏截图均为1640×800，已检查中文对白、选择、补给输入、海上HUD、伙伴及舰队面板。日志可滚动阅读后续步骤；截图默认停在列表顶部，章节完成另由实际存档与界面文字断言确认。最终截图和日志在本机工作树的 `.superpowers/sdd/2026-09-06-m1-first-voyage/final-acceptance/`（忽略目录）归档：

- `chinese-duke-dialogue.png`、`chinese-yes-no-choice.png`、`chinese-provision-input.png`、`chinese-sea-hud.png`
- `chinese-journal-at-sea.png`、`chinese-chapter-complete.png`
- `four-member-roster.png`、`four-ship-fleet.png`
- `verify.log`、`e2e-before-backdrop-fix.log`、`e2e-legacy-focused.log`、`e2e-final-focused.log`

## 审查与限制

各任务独立审查均已通过。整分支审查覆盖基线至`6a99cff`，提出上述方向键锁住和第4艘船越界两个P2；`325c882`修补的定向复审已通过，无新增问题。`c07b34d`遮罩修补另经定向复审通过。代码审查及完整旅程验收均已完成。交付保留独立工作树及M1预览，无共享主分支合并、推送或线上发布。

构建保留已有资源体积提示。Cypress自带的旧macOS终端尺寸辅助程序报告CPU架构提示，Edge测试本身正常运行；关闭自动清理旧截图，避免同一辅助依赖的系统废纸篓错误。
